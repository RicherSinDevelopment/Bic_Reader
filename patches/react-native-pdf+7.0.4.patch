diff --git a/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js b/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js
index 43fea3b..d3bf976 100644
--- a/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js
+++ b/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js
@@ -35,14 +35,28 @@
  |}>;

  interface NativeCommands {
-  +setNativePage: (
+ +setNativePage: (
     viewRef: React.ElementRef<ComponentType>,
     page: Int32,
   ) => void;
+  +setNativeHighlight: (
+    viewRef: React.ElementRef<ComponentType>,
+    page: Int32,
+    left: Float,
+    top: Float,
+    right: Float,
+    bottom: Float,
+    pageWidth: Float,
+    pageHeight: Float,
+    red: Float,
+    green: Float,
+    blue: Float,
+  ) => void;
+  +clearNativeHighlight: (viewRef: React.ElementRef<ComponentType>) => void;
 }

 export const Commands: NativeCommands = codegenNativeCommands<NativeCommands>({
-  supportedCommands: ['setNativePage'],
+  supportedCommands: ['setNativePage', 'setNativeHighlight', 'clearNativeHighlight'],
 });

  export default codegenNativeComponent<NativeProps>('RNPDFPdfView');
diff --git a/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js.rej b/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js.rej
new file mode 100644
index 0000000..cebbf6e
--- /dev/null
+++ b/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js.rej
@@ -0,0 +1,31 @@
+@@ -35,14 +35,28 @@
+  |}>;
+
+  interface NativeCommands {
+-  +setNativePage: (
++ +setNativePage: (
+     viewRef: React.ElementRef<ComponentType>,
+     page: Int32,
+   ) => void;
++  +setNativeHighlight: (
++    viewRef: React.ElementRef<ComponentType>,
++    page: Int32,
++    left: Float,
++    top: Float,
++    right: Float,
++    bottom: Float,
++    pageWidth: Float,
++    pageHeight: Float,
++    red: Float,
++    green: Float,
++    blue: Float,
++  ) => void;
++  +clearNativeHighlight: (viewRef: React.ElementRef<ComponentType>) => void;
+ }
+
+ export const Commands: NativeCommands = codegenNativeCommands<NativeCommands>({
+-  supportedCommands: ['setNativePage'],
++  supportedCommands: ['setNativePage', 'setNativeHighlight', 'clearNativeHighlight'],
+ });
+
+  export default codegenNativeComponent<NativeProps>('RNPDFPdfView');
diff --git a/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js.rej.orig b/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js.rej.orig
new file mode 100644
index 0000000..cebbf6e
--- /dev/null
+++ b/node_modules/react-native-pdf/fabric/RNPDFPdfNativeComponent.js.rej.orig
@@ -0,0 +1,31 @@
+@@ -35,14 +35,28 @@
+  |}>;
+
+  interface NativeCommands {
+-  +setNativePage: (
++ +setNativePage: (
+     viewRef: React.ElementRef<ComponentType>,
+     page: Int32,
+   ) => void;
++  +setNativeHighlight: (
++    viewRef: React.ElementRef<ComponentType>,
++    page: Int32,
++    left: Float,
++    top: Float,
++    right: Float,
++    bottom: Float,
++    pageWidth: Float,
++    pageHeight: Float,
++    red: Float,
++    green: Float,
++    blue: Float,
++  ) => void;
++  +clearNativeHighlight: (viewRef: React.ElementRef<ComponentType>) => void;
+ }
+
+ export const Commands: NativeCommands = codegenNativeCommands<NativeCommands>({
+-  supportedCommands: ['setNativePage'],
++  supportedCommands: ['setNativePage', 'setNativeHighlight', 'clearNativeHighlight'],
+ });
+
+  export default codegenNativeComponent<NativeProps>('RNPDFPdfView');
diff --git a/node_modules/react-native-pdf/index.d.ts b/node_modules/react-native-pdf/index.d.ts
index cfcea72..fc79079 100644
--- a/node_modules/react-native-pdf/index.d.ts
+++ b/node_modules/react-native-pdf/index.d.ts
@@ -66,7 +66,9 @@ export interface PdfProps {
 }

 export interface PdfRef {
-    setPage(pageNumber: number): void
+    setPage(pageNumber: number): void;
+    setHighlight(pageNumber: number, left: number, top: number, right: number, bottom: number, pageWidth: number, pageHeight: number, red: number, green: number, blue: number): void;
+    clearHighlight(): void;
 }

 declare const Pdf: React.ForwardRefExoticComponent<PdfProps & React.RefAttributes<PdfRef>>
diff --git a/node_modules/react-native-pdf/index.d.ts.rej b/node_modules/react-native-pdf/index.d.ts.rej
new file mode 100644
index 0000000..3812dc5
--- /dev/null
+++ b/node_modules/react-native-pdf/index.d.ts.rej
@@ -0,0 +1,11 @@
+@@ -66,7 +66,9 @@
+ }
+
+ export interface PdfRef {
+-    setPage(pageNumber: number): void
++    setPage(pageNumber: number): void;
++    setHighlight(pageNumber: number, left: number, top: number, right: number, bottom: number, pageWidth: number, pageHeight: number, red: number, green: number, blue: number): void;
++    clearHighlight(): void;
+ }
+
+ declare const Pdf: React.ForwardRefExoticComponent<PdfProps & React.RefAttributes<PdfRef>>
diff --git a/node_modules/react-native-pdf/index.d.ts.rej.orig b/node_modules/react-native-pdf/index.d.ts.rej.orig
new file mode 100644
index 0000000..3812dc5
--- /dev/null
+++ b/node_modules/react-native-pdf/index.d.ts.rej.orig
@@ -0,0 +1,11 @@
+@@ -66,7 +66,9 @@
+ }
+
+ export interface PdfRef {
+-    setPage(pageNumber: number): void
++    setPage(pageNumber: number): void;
++    setHighlight(pageNumber: number, left: number, top: number, right: number, bottom: number, pageWidth: number, pageHeight: number, red: number, green: number, blue: number): void;
++    clearHighlight(): void;
+ }
+
+ declare const Pdf: React.ForwardRefExoticComponent<PdfProps & React.RefAttributes<PdfRef>>
diff --git a/node_modules/react-native-pdf/index.js b/node_modules/react-native-pdf/index.js
index f807178..069f182 100644
--- a/node_modules/react-native-pdf/index.js
+++ b/node_modules/react-native-pdf/index.js
@@ -368,6 +368,18 @@ export default class Pdf extends Component {

     }

+    setHighlight(pageNumber, left, top, right, bottom, pageWidth, pageHeight, red, green, blue) {
+        if (!this._root) return;
+        PdfViewCommands.setNativeHighlight(
+            this._root, pageNumber, left, top, right, bottom, pageWidth, pageHeight, red, green, blue,
+        );
+    }
+
+    clearHighlight() {
+        if (!this._root) return;
+        PdfViewCommands.clearNativeHighlight(this._root);
+    }
+
     _onChange = (event) => {
         // Handle direct events for text selection/highlight
         if (event.nativeEvent.type) {
diff --git a/node_modules/react-native-pdf/index.js.rej b/node_modules/react-native-pdf/index.js.rej
new file mode 100644
index 0000000..7ab04a6
--- /dev/null
+++ b/node_modules/react-native-pdf/index.js.rej
@@ -0,0 +1,19 @@
+@@ -368,6 +368,18 @@
+
+     }
+
++    setHighlight(pageNumber, left, top, right, bottom, pageWidth, pageHeight, red, green, blue) {
++        if (!this._root) return;
++        PdfViewCommands.setNativeHighlight(
++            this._root, pageNumber, left, top, right, bottom, pageWidth, pageHeight, red, green, blue,
++        );
++    }
++
++    clearHighlight() {
++        if (!this._root) return;
++        PdfViewCommands.clearNativeHighlight(this._root);
++    }
++
+     _onChange = (event) => {
+         // Handle direct events for text selection/highlight
+         if (event.nativeEvent.type) {
diff --git a/node_modules/react-native-pdf/index.js.rej.orig b/node_modules/react-native-pdf/index.js.rej.orig
new file mode 100644
index 0000000..7ab04a6
--- /dev/null
+++ b/node_modules/react-native-pdf/index.js.rej.orig
@@ -0,0 +1,19 @@
+@@ -368,6 +368,18 @@
+
+     }
+
++    setHighlight(pageNumber, left, top, right, bottom, pageWidth, pageHeight, red, green, blue) {
++        if (!this._root) return;
++        PdfViewCommands.setNativeHighlight(
++            this._root, pageNumber, left, top, right, bottom, pageWidth, pageHeight, red, green, blue,
++        );
++    }
++
++    clearHighlight() {
++        if (!this._root) return;
++        PdfViewCommands.clearNativeHighlight(this._root);
++    }
++
+     _onChange = (event) => {
+         // Handle direct events for text selection/highlight
+         if (event.nativeEvent.type) {
diff --git a/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.h b/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.h
index 723ca7b..a9e09c9 100644
--- a/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.h
+++ b/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.h
@@ -60,6 +60,18 @@ UIView

 @property(nonatomic, copy) RCTBubblingEventBlock onTextSelectionChange;

+- (void)setNativeHighlight:(NSInteger)page
+                      left:(float)left
+                       top:(float)top
+                     right:(float)right
+                    bottom:(float)bottom
+                 pageWidth:(float)pageWidth
+                pageHeight:(float)pageHeight
+                       red:(float)red
+                     green:(float)green
+                      blue:(float)blue;
+- (void)clearNativeHighlight;
+
 @end

 #endif /* RNPDFPdfView_h */
diff --git a/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.h.rej b/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.h.rej
new file mode 100644
index 0000000..af8531b
--- /dev/null
+++ b/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.h.rej
@@ -0,0 +1,19 @@
+@@ -60,6 +60,18 @@
+
+ @property(nonatomic, copy) RCTBubblingEventBlock onTextSelectionChange;
+
++- (void)setNativeHighlight:(NSInteger)page
++                      left:(float)left
++                       top:(float)top
++                     right:(float)right
++                    bottom:(float)bottom
++                 pageWidth:(float)pageWidth
++                pageHeight:(float)pageHeight
++                       red:(float)red
++                     green:(float)green
++                      blue:(float)blue;
++- (void)clearNativeHighlight;
++
+ @end
+
+ #endif /* RNPDFPdfView_h */
diff --git a/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.mm b/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.mm
index de46c98..d098055 100644
--- a/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.mm
+++ b/node_modules/react-native-pdf/ios/RNPDFPdf/RNPDFPdfView.mm
@@ -75,6 +75,12 @@ @implementation RNPDFPdfView
     UIPinchGestureRecognizer *_pinchRecognizer;
     UILongPressGestureRecognizer *_longPressRecognizer;
     UITapGestureRecognizer *_doubleTapEmptyRecognizer;
+    PDFSelection *_switchHighlightSelection;
+    PDFAnnotation *_switchHighlightAnnotation;
+    PDFPage *_switchHighlightPage;
+    CADisplayLink *_switchHighlightDisplayLink;
+    CFTimeInterval _switchHighlightPulseStart;
+    UIColor *_switchHighlightColor;
 }

 #ifdef RCT_NEW_ARCH_ENABLED
@@ -234,6 +240,187 @@ - (void)setNativePage:(NSInteger)page
     [self didSetProps:[NSArray arrayWithObject:@"page"]];
 }

+- (void)setNativeHighlight:(NSInteger)page
+                      left:(float)left
+                       top:(float)top
+                     right:(float)right
+                    bottom:(float)bottom
+                 pageWidth:(float)pageWidth
+                pageHeight:(float)pageHeight
+                       red:(float)red
+                     green:(float)green
+                      blue:(float)blue
+{
+    [self clearNativeHighlight];
+    if (!_pdfDocument || page < 1 || page > (NSInteger)_pdfDocument.pageCount) {
+        RLog(@"switch highlight rejected: document=%@ page=%ld pageCount=%lu",
+             _pdfDocument ? @"loaded" : @"missing",
+             (long)page,
+             (unsigned long)_pdfDocument.pageCount);
+        return;
+    }
+
+    PDFPage *pdfPage = [_pdfDocument pageAtIndex:page - 1];
+    CGRect crop = [pdfPage boundsForBox:kPDFDisplayBoxCropBox];
+    if (pageWidth <= 0.0 || pageHeight <= 0.0 || CGRectIsEmpty(crop)) {
+        RLog(@"switch highlight rejected: invalid source/crop size source=%@ crop=%@",
+             NSStringFromCGSize(CGSizeMake(pageWidth, pageHeight)),
+             NSStringFromCGRect(crop));
+        return;
+    }
+
+    CGFloat scaleX = CGRectGetWidth(crop) / pageWidth;
+    CGFloat scaleY = CGRectGetHeight(crop) / pageHeight;
+    CGFloat clampedLeft = MIN(MAX(0.0, left), pageWidth);
+    CGFloat clampedRight = MIN(MAX(clampedLeft, right), pageWidth);
+    CGFloat clampedTop = MIN(MAX(0.0, top), pageHeight);
+    CGFloat clampedBottom = MIN(MAX(clampedTop, bottom), pageHeight);
+    CGRect wordRect = CGRectMake(
+        CGRectGetMinX(crop) + clampedLeft * scaleX,
+        CGRectGetMaxY(crop) - clampedBottom * scaleY,
+        MAX(1.0, (clampedRight - clampedLeft) * scaleX),
+        MAX(1.0, (clampedBottom - clampedTop) * scaleY)
+    );
+    wordRect = CGRectIntersection(wordRect, crop);
+    if (CGRectIsNull(wordRect) || CGRectIsEmpty(wordRect)) {
+        RLog(@"switch highlight rejected: mapped rect outside crop source=%@ crop=%@",
+             NSStringFromCGRect(CGRectMake(left, top, right - left, bottom - top)),
+             NSStringFromCGRect(crop));
+        return;
+    }
+
+    CGFloat selectionPadding = MAX(2.0, CGRectGetHeight(wordRect) * 0.25);
+    PDFSelection *selection = [pdfPage selectionForRect:CGRectInset(
+        wordRect, -selectionPadding, -selectionPadding
+    )];
+    if (selection.string.length == 0) {
+        selection = [pdfPage selectionForWordAtPoint:CGPointMake(
+            CGRectGetMidX(wordRect), CGRectGetMidY(wordRect)
+        )];
+    }
+
+    CGRect highlightRect = wordRect;
+    if (selection.string.length > 0) {
+        CGRect selectedBounds = [selection boundsForPage:pdfPage];
+        if (!CGRectIsNull(selectedBounds) && !CGRectIsEmpty(selectedBounds)) {
+            CGRect clippedSelection = CGRectIntersection(selectedBounds, crop);
+            if (!CGRectIsNull(clippedSelection) && !CGRectIsEmpty(clippedSelection)) {
+                highlightRect = clippedSelection;
+            }
+        }
+        _switchHighlightColor = [UIColor colorWithRed:MIN(MAX(red, 0.0), 1.0)
+                                                green:MIN(MAX(green, 0.0), 1.0)
+                                                 blue:MIN(MAX(blue, 0.0), 1.0)
+                                                alpha:1.0];
+        selection.color = [_switchHighlightColor colorWithAlphaComponent:0.50];
+        _switchHighlightSelection = selection;
+    }
+
+    if (!_switchHighlightColor) {
+        _switchHighlightColor = [UIColor colorWithRed:MIN(MAX(red, 0.0), 1.0)
+                                                green:MIN(MAX(green, 0.0), 1.0)
+                                                 blue:MIN(MAX(blue, 0.0), 1.0)
+                                                alpha:1.0];
+    }
+
+    PDFAnnotation *annotation = [[PDFAnnotation alloc]
+        initWithBounds:CGRectInset(highlightRect, -1.5, -1.0)
+        forType:PDFAnnotationSubtypeHighlight
+        withProperties:nil];
+    annotation.color = [_switchHighlightColor colorWithAlphaComponent:0.88];
+    annotation.shouldDisplay = YES;
+    annotation.shouldPrint = NO;
+    [pdfPage addAnnotation:annotation];
+    _switchHighlightAnnotation = annotation;
+    _switchHighlightPage = pdfPage;
+
+    // Animate only the annotation color. Keeping its bounds fixed avoids the
+    // word marker drifting or changing size while PDFKit scrolls into place.
+    _switchHighlightPulseStart = 0.0;
+    _switchHighlightDisplayLink = [CADisplayLink
+        displayLinkWithTarget:self
+        selector:@selector(updateSwitchHighlightPulse:)];
+    _switchHighlightDisplayLink.preferredFramesPerSecond = 30;
+    [_switchHighlightDisplayLink addToRunLoop:NSRunLoop.mainRunLoop
+                                      forMode:NSRunLoopCommonModes];
+
+    // Keep enough page content above the marker so PDFKit does not align the
+    // highlighted word against the top edge of the visible document area.
+    // PDF coordinates grow upward, so extend the navigation rectangle much
+    // farther above the word than below it.
+    CGRect revealRect = CGRectMake(
+        CGRectGetMinX(highlightRect) - 8.0,
+        CGRectGetMinY(highlightRect) - 20.0,
+        CGRectGetWidth(highlightRect) + 16.0,
+        CGRectGetHeight(highlightRect) + 116.0
+    );
+    revealRect = CGRectIntersection(revealRect, crop);
+    [_pdfView goToRect:revealRect onPage:pdfPage];
+    [_pdfView setNeedsDisplay];
+    RLog(@"switch highlight applied: page=%ld text=%@ sourceSize=%@ rect=%@",
+         (long)page,
+         selection.string ?: @"<no PDFKit text; rectangle fallback>",
+         NSStringFromCGSize(CGSizeMake(pageWidth, pageHeight)),
+         NSStringFromCGRect(highlightRect));
+}
+
+- (void)updateSwitchHighlightPulse:(CADisplayLink *)displayLink
+{
+    if (displayLink != _switchHighlightDisplayLink || !_switchHighlightAnnotation) {
+        [displayLink invalidate];
+        return;
+    }
+
+    if (_switchHighlightPulseStart <= 0.0) {
+        _switchHighlightPulseStart = displayLink.timestamp;
+    }
+
+    BOOL reduceMotion = UIAccessibilityIsReduceMotionEnabled();
+    CFTimeInterval duration = reduceMotion ? 0.8 : 1.2;
+    CGFloat progress = MIN(1.0, MAX(0.0,
+        (displayLink.timestamp - _switchHighlightPulseStart) / duration));
+    CGFloat alpha = 0.0;
+
+    if (reduceMotion) {
+        alpha = 0.72 * (1.0 - progress);
+    } else if (progress < 0.30) {
+        CGFloat rise = progress / 0.30;
+        alpha = 0.78 + (0.17 * rise);
+    } else {
+        CGFloat fade = (progress - 0.30) / 0.70;
+        CGFloat easedFade = fade * fade * (3.0 - 2.0 * fade);
+        alpha = 0.95 * (1.0 - easedFade);
+    }
+
+    _switchHighlightAnnotation.color =
+        [_switchHighlightColor colorWithAlphaComponent:alpha];
+    [_pdfView setNeedsDisplay];
+
+    if (progress >= 1.0) {
+        [self clearNativeHighlight];
+    }
+}
+
+- (void)clearNativeHighlight
+{
+    [_switchHighlightDisplayLink invalidate];
+    _switchHighlightDisplayLink = nil;
+    _switchHighlightPulseStart = 0.0;
+    BOOL hadHighlight = _switchHighlightSelection || _switchHighlightAnnotation;
+    if (_switchHighlightAnnotation && _switchHighlightPage) {
+        [_switchHighlightPage removeAnnotation:_switchHighlightAnnotation];
+    }
+    _switchHighlightAnnotation = nil;
+    _switchHighlightPage = nil;
+    if (_switchHighlightSelection) {
+        _switchHighlightSelection = nil;
+    }
+    if (hadHighlight) {
+        [_pdfView setNeedsDisplay];
+    }
+}
+
 #endif

 - (instancetype)initWithBridge:(RCTBridge *)bridge
@@ -600,6 +787,7 @@ - (void)notifyOnChangeWithMessage:(NSString *)message

 - (void)dealloc{

+    [self clearNativeHighlight];
     _pdfDocument = Nil;
     _pdfView = Nil;
